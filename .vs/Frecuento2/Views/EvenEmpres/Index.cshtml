@model IEnumerable<Frecuento2.Models.EvenEmpre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Precio_Base)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empresa.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Evento.Descripción)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Precio_Base)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empresa.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Evento.Descripción)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_EvenEmpre }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_EvenEmpre }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_EvenEmpre })
        </td>
    </tr>
}

</table>
