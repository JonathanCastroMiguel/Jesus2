@model IEnumerable<Frecuento2.Models.Tipo_Evento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripción)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripción)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Tipo_Evento }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Tipo_Evento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Tipo_Evento })
        </td>
    </tr>
}

</table>
